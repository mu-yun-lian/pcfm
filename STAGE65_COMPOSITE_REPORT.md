# 阶段 6.5：已验证复合预测视图

## 阶段结论

本阶段把稳定人物模型和一个已经通过发现、独立选择、封存确认及适用域门禁的预测机制，绑定为单一的签名预测身份。它解决的是“下游到底使用哪个预测模型”的工程与可证伪性问题，不把数值结构提升为人的真实信念、价值或因果机制。

实现版本：

- 包：`0.9.1`
- 复合工件：`pcfm-composite-model-v1`
- 复合预测视图：`composite-predictive-view-v1`
- 主动实验计划：`pcfm-active-experiment-v3`

## 可检验主张

在相同基础模型、机制计划、机制报告及三段签名证据下：

1. 复合工件身份可确定性重算且与输入顺序无关；
2. 正式复合预测与已确认机制预测逐值一致；
3. 主动实验候选的显示概率来自同一联合预测视图；
4. 修改、重签或替换派生工件不能改变原始证据重算结果；
5. 超出机制作用域、超过有效期或试图直接叠加旧动态状态时拒绝输出。

## 实现结构

`CompositeModelArtifact` 绑定：

- 基础模型、人物和验证者身份；
- 机制计划、报告及所选候选；
- 发现、选择、确认三段数据摘要；
- 创建时间和由基础/机制共同确定的最早失效时间；
- 解释边界与不确定性范围；
- 内容哈希和 HMAC-SHA256 签名。

`predict_with_composite_model` 每次都会：

1. 验证复合工件签名；
2. 从三份原始签名账本重跑机制报告；
3. 重建复合工件并逐字段比较；
4. 检查预测时间、领域、选项、上下文、全局/局部支持；
5. 使用已确认机制的正式部署概率核输出结果。

复合主动规划把基础参数与机制修正参数拼接为联合高斯视图。候选概率使用正式部署的 Logistic-normal 近似；互信息仍用确定性 Gauss–Hermite 积分。`pcfm-active-experiment-v3` 绑定 `predictive_model_id`、预测版本和参数维度，避免下游把基础计划与复合计划混为一谈。

修复审计后，稳定预测、动态预测、机制确认/部署和主动规划都调用 `math_utils.logistic_normal_probability`，不再各自复制概率公式。公共预测及任意大小候选池的单次规划只从原始机制证据重算一次报告。

## 更新语义

复合主动计划不能交给旧的基础模型更新路径静默执行。`apply_active_experiment_results` 会返回：

`composite_active_plan_requires_composite_update`

复合专用入口 `apply_composite_active_experiment_results` 和 CLI `apply-composite-experiment` 会验证复合计划与结果、要求更晚的独立验证账本、更新基础模型，并返回 `base_updated_composite_invalidated`。新基础模型绑定原复合主动计划 ID；旧机制报告和复合身份明确失效。随后仍必须用新的独立证据重新比较/确认机制，不能把更新基础模型误称为已经更新了复合模型。

候选池不得复用基础谱系或三段机制证据中的 trial/design；即使只改变场景 ID，内容指纹仍会拒绝。更新后的验证账本同样不得复用机制证据。

## 验证

执行：

```powershell
$env:PYTHONPATH = "$PWD\src"
python -m compileall -q src tests
python -m unittest tests.test_stage65_composite -v
python -m unittest tests.test_stage65_repair -v
python -m unittest discover -s tests -v
```

结果：

- 编译检查：通过；
- 阶段 6.5 原专项测试：14/14；
- 修复审计测试：6/6；
- 全量回归：138/138。

新增修复测试覆盖机制证据场景重放、改名设计重放、更新验证集重放、单次机制重算、不可关闭的信息增益下限、复合结果吸收/失效，以及规划和更新 CLI。

## 仍未解决

- 复合预测只支持一个已确认机制组件。
- 联合协方差采用分块对角近似，未估计基础参数与修正参数的后验相关。
- 候选选择、候选集合遗漏和模型形式不确定性未进入概率区间。
- 动态状态 v2 相对基础模型残差推断；必须针对复合预测核重新推断后才能联合使用。
- 原始账本的外部完整性、真实时间和现实人物代表性仍需透明日志、可信时间戳和独立采集治理。
- 任何通过结果只支持限定任务内的预测结构，不支持心理、意识或因果解释。
