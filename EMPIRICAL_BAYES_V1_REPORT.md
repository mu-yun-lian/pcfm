# Anisotropic Empirical-Bayes Adapter v1 实验报告

## 结论

候选状态：`rejected_candidate`。

该候选显著优于已淘汰的 HyperNetwork，也在五个新封存种子上取得正的平均改善，但只有 `3/5` 种子通过全部预注册门槛，低于至少 `4/5` 的要求。因此不能替代当前 `personal_map_logistic` 主基线。

## 实现

本候选不训练神经网络。它：

1. 拟合群体 Logistic；
2. 对 20 个元训练人物分别拟合弱先验 MAP；
3. 从人物参数样本协方差中扣除平均后验协方差；
4. 对特征值应用 `0.02` 下限；
5. 用 4 个 validation 人物从 `0/.25/.5/.75/1` 中选择协方差收缩；
6. 对新人物使用选中协方差做各向异性 MAP；
7. 通过共享 `LogisticChoiceModel.probabilities` 部署。

它只有一个 6×6 对称协方差工件，共 21 个独立协方差标量；CPU、NumPy、单进程，无 GPU。

## 新封存种子结果

以下种子在算法、候选集合与门槛固定后首次运行。主指标为 64 条 support 后的 `scenario_test` NLL。

| 种子 | 群体 | 各向同性 MAP | 各向异性 EB | EB 改善 | 错误人物 EB | 收缩 | 状态 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 8101 | 0.579184 | 0.378158 | 0.338279 | 0.039879 | 0.864651 | 0.50 | pass |
| 8102 | 0.433263 | 0.348846 | 0.330157 | 0.018689 | 0.497982 | 1.00 | pass |
| 8103 | 0.576341 | 0.454562 | 0.455509 | -0.000947 | 0.747401 | 1.00 | fail |
| 8104 | 0.714285 | 0.549006 | 0.557387 | -0.008381 | 0.799567 | 0.50 | fail |
| 8105 | 0.563849 | 0.386421 | 0.350010 | 0.036411 | 0.719475 | 1.00 | pass |

五种子平均 NLL 改善为 `0.017130`，超过 `0.005` 平均门槛；但逐种子稳定性只有 `3/5`，因此最终失败。

其他门：

- 所有种子均明显优于错误人物 support；
- 所有种子的时间测试均未超过允许劣化线；
- 零异质性相对群体 NLL 差为 `0.007206`，通过 `0.04` 上限；
- 稳定非线性错设 NLL 为 `0.708741`，绝对充分性门拒绝确认；
- OOD 不参与接受判定。

## 为什么没有通过

经验贝叶斯先验在部分数据集上明显减少了固定精度 4 的过度收缩，因此平均表现很好。但 20 个元训练人物仍不足以保证六维协方差在每个新人群样本上都优于简单各向同性先验。

种子 8103 和 8104 的轻微负增益表明：validation 选择的协方差结构仍会受到有限人物样本和有限 validation 人物的影响。平均改善不能替代逐环境稳定性，因此不能用 `3/5` 的结果冒充“整体成功”。

## 已通过的工程门禁

- 群体、元训练、validation、support 与测试答案角色分离；
- 翻转全部测试人物答案不改变先验工件；
- 零 support 精确退化为群体模型；
- 错误人物 support 独立对照；
- support 顺序和无意义 ID 改名不改变预测头；
- 改名后的近重复 support 被拒绝；
- 跨域 support 被拒绝；
- 协方差和精度有限、对称、正定且互为逆；
- 阈值、种子、候选集合与特征值下限不能调弱；
- 工件可序列化、检出篡改并从训练证据确定性重算；
- 稳定错设、零异质性、OOD 隔离和 CLI 路径均已测试。

## 仍未解决

- 真实人物外部效度；
- 真实未来证据、时间过期和可信时间戳；
- 外部试验完整性注册；
- 局部 OOD 支持；
- 协方差方向的心理语义；
- 更多领域和开放文本任务；
- 人物数量增加后协方差估计是否稳定。

工程状态只能标记为 `implemented_exploratory`，统计候选状态为 `rejected_candidate`。

## 验证结果

- 语法与导入检查：通过；
- Empirical-Bayes 聚焦测试：`17/17`，耗时 `21.079s`；
- 全项目回归：`183/183`，耗时 `271.131s`；
- 模块确认门禁：按设计拒绝。拒绝原因是本模块仍为探索状态，且没有真实未来留出证据、全局/局部适用性证明和时间过期证据。

测试全绿只说明实现满足已声明合同，不会覆盖统计审计的 `3/5` 失败结论。

## 复现

```powershell
$env:PYTHONPATH = (Resolve-Path src).Path
python -m pcfm empirical-bayes-v1
python -m unittest discover -s tests -p "test_empirical_bayes_v1.py" -v
python -m unittest discover -s tests -v
```
