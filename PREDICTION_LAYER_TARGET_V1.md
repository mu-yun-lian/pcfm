# PCFM 人物预测层目标标准 v1

本标准冻结本轮最多五次 Loop 的通过条件。它约束人物内容预测层，不把界面、归档或表达风格功能算作预测层完成度，也不把软件测试当作真实人物准确性。

## 唯一目标链路

原始材料 -> 可追溯事件原子 -> 单事件伴随倾向 -> 同领域/跨领域公开反应倾向与人物已展示知识 -> 结合多轮上下文分析新问题 -> 同类事件、领域倾向、整体倾向或非人物通用知识路径 -> 冻结内容合同 -> 独立风格渲染。

## 硬性验收项

1. **材料自动处理**：问答和非问答材料都能生成候选；模型抽取只能产生候选，不能自行确认事实。
2. **事件原子完整性**：人物、来源、原文、触发、对象、领域、角色、受众、场合、时间、当时已知信息、公开反应均有明确字段；缺失项写为 `unknown`，并生成按用途的允许/禁止清单。
3. **时间不是单字段**：至少区分回答时间、材料发布时间或其可用代理、时间精度和来源；无时间事件不得进入时间变化、封存验证或现实优化训练。
4. **分层多标签分类**：事件可属于多个领域；英文关键词必须按词边界匹配；中文必须产生可检索的多字主题，分类候选保留依据。
5. **伴随倾向可检验**：每条只描述公开反应，包含对象、方向、条件、理由/取舍、例外、领域、时间范围、证据和反证；单事件不得冒充稳定价值观。
6. **多事件聚合有来源边界**：记录事件数、独立来源数、领域数、时间范围、支持和反证；转载或同一来源多段不能伪装成多份独立证据；冲突不得简单平均。
7. **知识分离**：人物已公开展示使用的主张与外部大模型通用知识分开；前者不自动等于事实，后者不自动等于人物知识。
8. **统一分层预测**：同一入口先解析当前问题和上下文，再依次尝试直接/同类事件、同领域倾向、整体倾向、非人物通用知识；每条路径输出依据、适用性和降级原因。
9. **检索适用性**：同类事件必须同时通过语义、领域/对象和上下文门禁；低词汇重合不能把无关事件复制进答案。
10. **大模型权限受限**：大模型可做候选抽取、问题分析、通用知识和措辞；不能新增人物立场、人物事实或绕过证据门禁。内容预测与风格渲染分离。
11. **现实回答优化隔离**：只有用户选中且审核通过的相近现实回答可成为待优化候选；封存资料、无可靠顺序资料和未确认资料不能直接训练；版本可比较和回滚。
12. **准确性诚实状态**：必须提供正确人物、错误人物、群体/频率和检索基线的时间外评测入口；没有足够真实封存样本时状态固定为 `not_assessed` 或 `insufficient_evidence`。

## 本轮通过定义

- 上述工程合同均有运行时代码和行为测试，不是只写报告或只存字段；
- 预测、评测、对话入口读取同一人物模型视图；
- 聚焦测试和完整回归通过；
- 真实人物准确性若无足够数据，作为明确外部缺口保留，不阻止工程对齐，但禁止宣称预测准确。

