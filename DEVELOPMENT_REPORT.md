# PCFM MVP v0.5.1 修复报告

> 历史说明：本文记录 bundle v5 阶段。当前 v0.9.1 使用 bundle v7；现行接口以 README 和第四至第 6.5 阶段报告为准。

## 修复来源

v0.5 的常规测试全部通过，但对抗审核发现四个核心问题：

1. 时间模块把稳定的长期偏差误报为漂移；
2. 单一马氏椭球接受多峰支持域内部空洞；
3. 最终验证特征参与 OOD 画像；
4. 覆盖验证门禁后，预测结果看不出门禁被绕过。

另外发现跨领域区间没有变化、模型无时效限制、选项和上下文未进入边界判断等问题。

## 已完成修复

### 数据隔离

工作流现在要求训练、适用性校准和最终验证三份账本。三方事件 ID 和人物/场景键两两不重叠。模型参数只来自训练账本；支持域只来自适用性账本；最终验证账本不参与任何参数或 OOD 阈值拟合。

### 局部支持域

保留全局马氏距离，并新增 kNN 局部距离：

- 适用性数据按人物—场景身份哈希做顺序无关的确定性拆分，分别形成参考点与校准点；
- 每个校准点计算到参考点的第 5 近邻距离；
- 经验高分位阈值应用 1.25 安全因子；
- 新点同时接受全局和局部门禁。

环形合成数据的中心点全局分数接近零，但会被 `local_support_gap` 拒绝。

### 时间两样本检验

新算法比较每个候选时间边界两侧的 score 均值差，不再只检验后期 score 是否偏离零。

多候选切分和多特征通过 Bonferroni 临界值控制。稳定系统偏差反例的早晚 NLL 完全相同，现在判为 `stable`；后期行为反转仍判为 `unstable`。

### 时效和语义元数据

- 非诊断预测必须提供时间；
- 早于参考数据或超过 180 天拒绝；
- 未见领域、选项或上下文触发外推警告；
- 外推时撤销条件参数区间并标记模型形式不确定性无法量化。

### 覆盖审计

验证覆盖和适用性覆盖分别授权。所有实际覆盖原因进入 `gate_overrides`，任一覆盖都撤销概率区间。CLI 对所有门禁拒绝使用统一 JSON 错误结构。

## 验证规则变化

时间稳定性为 `not_assessed` 时不再允许模型获得完整 `passed`。最终验证必须具有至少两个时间窗口，每侧至少 50 条记录。

## 存储变化

`pcfm-bundle-v5` 新增：

- 局部参考特征；
- 参考/校准样本数；
- kNN 邻居数与局部阈值；
- 支持的选项和上下文签名；
- 有效截止时间和最大年龄；
- 安全因子；
- 时间多重比较临界值；
- 预测覆盖和模型形式不确定性状态。

所有新增模型状态都受 `model_id` 摘要保护。

## 验证结果

- `python -m compileall -q src tests`：通过；
- `python -m unittest discover -s tests`：63/63 通过；
- 10 个随机种子的同分布新样本接受率：最低 99.09%，平均 99.82%；
- 10 个稳定时间序列：误报 0；
- 10 个明确行为漂移序列：检出 10；
- CLI 的拟合、预测时间和两类独立覆盖参数均已完成解析检查。

这些数字来自当前合成测试，只证明实现与预注册的合成反例一致，不是对真实人物数据性能的保证。

## 仍需后续解决

- 局部数值支持不等于概念支持；
- 固定 180 天期限需要真实纵向数据校准；
- 时间漂移原因尚不能归因；
- 真实人物数据可能多峰、稀疏且高维，需要任务专属的保形校准；
- 下一阶段的动态状态必须与长期人物参数分离，不能把检测到的变化直接写入稳定人格。
