# Person-choice Benchmark v1 实现报告

## 本阶段到底实现了什么

本阶段没有继续扩张“完整人物思维模型”的概念范围，而是建立了第一个可执行、可反驳的预测基准：

> 对从未参与群体训练的测试人物，给定 0、16、32 或 64 条支持记录，预测其在未见结构化场景和较晚时间场景中的二元选择概率。

输入被限制为五个预先声明的连续特征：收益、损失风险、延迟、控制感与公平性。结果只能解释为受控合成任务中的人物特异性预测，不能解释为恢复了真人的思想、价值观、人格、意识或因果心理机制。

## 数据与隔离

- 20 个元训练人物，每人 96 条记录；
- 4 个验证人物，每人 96 条记录；v1 参数已预先固定，因此该组保持未使用；
- 6 个最终测试人物；
- 每个测试人物有 64 条支持记录、48 条未见场景记录、48 条较晚时间记录和 24 条 OOD 诊断记录；
- 共 3,408 条记录；
- 人物组完全不重叠；
- 支持、场景测试、时间测试和 OOD 角色显式分离；
- 重复记录 ID、重复人物—场景、相同设计改名重放、错误时间方向和内容摘要不一致均被拒绝。

测试人物的答案不会进入群体模型或人物嵌入网络的训练；它们只用于各自支持集上的适配。

## 对照模型

1. `population_logistic`：只用元训练人物拟合的群体 Logistic。
2. `personal_map_logistic`：以群体模型为先验，只用正确测试人物支持集进行 MAP 适配。
3. `wrong_person_logistic`：故意把另一个测试人物的个体模型用于当前人物。
4. `person_embedding_mlp`：纯 NumPy 的小型人物嵌入 MLP；群体训练后，只优化新人物嵌入。

HyperNetwork 没有在本阶段实现。它被保留为下一候选，并且必须在相同数据隔离与评估核下同时超过 `personal_map_logistic` 和 `person_embedding_mlp`，否则不应进入主路径。

## 固定结果

种子：`7301`。数据集摘要：
`f8d5f6ee10fb367694258f30be52ff2ab230f022df92afb4af2b5d9fc4592e34`。

场景测试集结果：

| 支持数 | 群体 Logistic NLL | 个体 MAP NLL | 错配人物 NLL | 人物嵌入 MLP NLL |
|---:|---:|---:|---:|---:|
| 0 | 0.628307 | 0.628307 | 0.628307 | 1.038064 |
| 16 | 0.628307 | 0.544208 | 0.626389 | 0.871655 |
| 32 | 0.628307 | 0.501985 | 0.607945 | 0.743022 |
| 64 | 0.628307 | 0.466396 | 0.661764 | 0.678970 |

64 条支持记录时：

| 测试角色 | 群体 Logistic NLL | 个体 MAP NLL | 错配人物 NLL | 人物嵌入 MLP NLL |
|---|---:|---:|---:|---:|
| 未见场景 | 0.628307 | 0.466396 | 0.661764 | 0.678970 |
| 较晚时间 | 0.686771 | 0.446124 | 0.654548 | 0.690131 |
| OOD 诊断 | 1.830410 | 0.599908 | 0.880908 | 0.618988 |

OOD 数值只能用于诊断。因为合成生成器在 OOD 区域仍沿用同一个线性选择规律，这个结果不构成现实跨域泛化证据。

## 结论

正结果：在预先设定的异质性合成数据上，正确人物的支持集能带来稳定的概率预测增益，并优于人物错配对照。

种子 `7301` 至 `7305` 的五次确定性审计中，个体 MAP 相对群体 Logistic 的场景 NLL 改善依次为 `0.161911`、`0.131200`、`0.109583`、`0.148078` 和 `0.177866`，五次均超过 `0.05` 最低改善线。这排除了结果只依赖种子 `7301`，但仍不是统计置信区间。

负结果：人物嵌入 MLP 没有超过简单个体 MAP Logistic。复杂网络目前没有获得优先权，也没有证据表明嵌入维度对应任何心理构念。

零异质性反证也通过：当所有人物共享同一生成参数时，个体模型不会制造实质性的个体增益。

## 尚未解决

- 数据是合成的，尚无真人外部效度；
- 单一二选一领域不足以覆盖人的信念、价值、目标、记忆和开放文本推理；
- 已有五种子确定性审计，但没有跨种子置信区间或真实人物级统计功效分析；
- 尚未验证稳定的模型族错设条件；
- 数据摘要可检出内容篡改，但本基准产物尚未接入外部签名、可信时间戳和真实采集完整性系统；
- 报告是一次固定运行结果，尚未实现独立的报告反序列化与逐项重算命令；
- HyperNetwork 仍只是待检验候选，不是已证明方案。

## 复现

```powershell
$env:PYTHONPATH = (Resolve-Path src).Path
python -m pcfm benchmark-v1 --seed 7301
python -m unittest discover -s tests -p "test_person_choice_benchmark.py" -v
python -m unittest discover -s tests -v
```

完整机器可读结果保存在 `artifacts/person_choice_benchmark_v1.json`。
