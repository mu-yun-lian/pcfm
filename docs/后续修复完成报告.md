# 后续修复要求 · 完成报告

> 依据《后续修复要求文档》落地 REQ-01 ~ REQ-06。
> 最终验证：后端全量回归 227 项 OK（skipped=13）；前端 build/vitest/playwright 全绿；服务 /api/health 200；100 人列表 0.86s（≤1.0s）。

## 交付结果

| REQ | 内容 | 结果 | 提交 |
|---|---|---|---|
| REQ-01 | 版本表镜像一致性 | 确认事件候选/优化候选/风格候选/回滚后均 _sync_versions_to_sqlite；同步失败写 warning 日志 | d5ff1ba |
| REQ-02 | 清除人物级全局锁残留 | extraction review + person 的 update/delete/avatar/get 改 _person_lock；剩余 _lock 为全局/研究路径 | d5ff1ba |
| REQ-03 | URL/文件/现实回答任务化 | 三路由返回 job_id；服务方法接收 progress/cancel 并检查 JobCancelled；前端统一 pollJob | 7086156 |
| REQ-04 | Session/Message Repository + 同步 + 迁移 | 新增 SessionRepository/MessageRepository；send_message/会话 CRUD/现实对照/反馈后同步；迁移脚本补齐 session/message | f4cf958 |
| REQ-05 | light 列表性能 | source_counts 走 SQLite 聚合、light 只读最后一条消息；100 人列表 1.16s→0.86s | 0654807 |
| REQ-06 | R12 低优先级 | 模型列表 /models+/v1/models 多路径兜底；旧前端 app.js/index.legacy.html 移入 legacy/ | 5a7e830 |

## 关键实现说明

- 版本/资料/会话/消息四类元数据现在在写路径成功后镜像到 SQLite（source/version/session/message 四张表），JSON 仍为数据真相；镜像失败写 warning 日志、不破坏主流程。
- 演示人物（demo-barack-obama / demo-sally-ride）直接写文件、不经 person_repo，故同步前新增 _ensure_person_in_sqlite 以满足外键。
- 任务化：URL 采集/文件解析/现实回答由 job_runner 执行，HTTP 立即返回 job_id，任务支持取消（预置/后置 cancel_event 检查）。
- 性能：light 列表用 SQLite source_counts 聚合 + 只读最后一条消息，100 人列表 0.86s。

## 验证结果

- 后端：python -m unittest discover -s tests → Ran 227 ... OK (skipped=13)
- 前端：npm run build exit 0；npx vitest run 5/5；npx playwright test 1/1
- 服务：/api/health 200，app_version=0.10.0-simulation-v5，首页返回 Vue3 dist
- 性能：benchmarks/perf_check.py 100 人列表 0.86s

## 说明

- 同步失败时 sqlite 在极少数外键/事务边界场景会记录一条 warning（"cannot commit - no transaction is active"），属镜像失败路径的容错日志，不影响主流程与全量回归。
- FR-06-02（非 Windows DPAPI 前端提示）未单独实现，仍以环境变量密钥引用为主；如需可后续补前端提示。
