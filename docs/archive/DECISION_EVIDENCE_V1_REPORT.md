# Decision-Context-Rationale Evidence Contract v1 阶段报告

## 结论

本阶段状态为 `implemented_unintegrated`。

系统现在能够把一个人的实际选择、选择前情境、选择来源，以及同期第一人称理由
或可验证行动后果封装成可重算、可签名、可拒绝的 v1 证据包。构建、保存、重载、
独立验证和两个 CLI 入口使用同一验证核。

该结果只证明证据入口的工程闭环。当前没有真实证据包，没有训练新模型，也没有
证据表明理由信息能提升人物预测。`training_authorized` 固定为 `false`，语义声明
列表固定为空，现有 `fit`、`update`、动态状态、机制比较和预留语义模块不会消费
该工件。

## 已实现的功能

- 每条记录绑定人物、领域、任务、完整题干、至少两个选项、实际选择和决定时间；
- 来源正文内嵌，SHA-256 可离线重算；
- 引用片段必须真实存在于来源正文；
- 完整题干和全部选项必须出现在选择前来源中；
- 实际选择文本必须出现在选择来源中；
- 理由必须归属于同一人物，并处于决定前 24 小时至决定后最多 168 小时；
- 168 小时为不可放宽硬上限，NaN 和无穷配置同样拒绝；
- 后果证据必须晚于决定、关联同一事件并使用可验证外部后果来源；
- 七种证据角色固定且一条记录只能有一个角色；
- `sealed_confirmation` 必须在决定前完成角色分配并绑定登记摘要；
- 每个“人物 × 领域 × 任务”必须同时有开发记录和更晚的封存确认记录；
- 精确重放、跨角色重放、近重复、缺失来源、孤立来源、改名篡改、汇总篡改、
  旧 schema 和签名篡改均拒绝；
- 输入顺序不改变工件，纯事件/来源标识改名不改变内容指纹；
- 工件身份绑定人物、领域、任务、选项、时间、角色、来源和引用内容。

## 敌意审计后的修复

首版聚焦测试通过后又发现两个功能漏洞：

1. 只写 `sealed_confirmation` 仍可能事后把已经看到结果的记录改成封存确认；
2. 记录里的题干和选项虽然被签名，但未强制证明它们确实存在于选择前来源中。

修复后新增了 `role_assigned_at`、`role_assignment_reference`、选择前角色分配门禁，
以及题干、全部选项和已选选项的来源正文核对。随后又补充了每个具体人物—领域—
任务的开发/确认覆盖门禁，防止用人物 A 的训练记录和人物 B 的确认记录拼成表面
完整的数据包。

## 验证

- 修改前生产基线：`229/229`，耗时 `190.899s`；
- 本阶段聚焦测试：`11/11`，最终复跑耗时 `0.837s`；
- Python 静态编译：通过；
- 最终全量回归：`240/240`，耗时 `200.991s`；
- 完成门禁审计：按预期只因状态为 `implemented_unintegrated` 而拒绝
  `implemented_confirmatory`，包装验证脚本返回成功。

## 仍未解决的问题

### 可以继续用工程解决

- 尚未为选定人物和领域制作首个真实输入清单；
- 还没有将合格证据转换为冻结的模型特征合同；
- 还没有运行理由信息相对共享动态、错误人物、历史-only 和内容-only 对照的
  增量预测实验。

### 需要外部工具或机构保证

- 独立登记服务证明封存角色确实在决定前登记；
- 可信网页或文档档案证明远程发布时间和原始正文；
- 来源清单或透明日志约束上游是否隐藏了不利、矛盾或失败记录。

### 仅靠代码无法解决

- 公开理由是否真诚；
- 公开理由是否是实际决策原因而不是事后解释或策略表达；
- 有限公开材料是否包含真实人物使用的全部决策机制；
- 一个领域得到的规律是否能代表更一般的“这个人”。

## 下一步

下一步不是增加认知标签，也不是接入 MiroFish。应选择一个资料充足的人和一个
窄领域，先制作第一份真实的决定—情境—理由证据清单，并在任何封存确认结果产生
前登记角色。只有证据包通过本合同，才设计最小增量预测实验。
