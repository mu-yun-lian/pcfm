# 重构评价修复报告

> 依据《重构评价.md》逐项修复。本报告记录 R1–R12 的处理结果。
> 最终全量回归：227 项测试 OK（skipped=13），即 214 通过 + 13 项显式跳过。

## 已修复

| 编号 | 严重度 | 问题 | 修复 | 提交 |
|---|---|---|---|---|
| R1 | Critical | 停止生成不生效（cancel 仅置 flag，任务不检查） | send_message 贯穿 cancel_event，开始/持久化前检查并抛 JobCancelled；用户消息尽早持久化 | c8d39b9 |
| R2 | Critical | 单条提取按钮不轮询任务，误报「候选已生成」 | extractSource 改为 pollJob(job_id) 完成后再提示 | c8d39b9 |
| R6 | High | conversation 数据损坏致 500/启动失败 | conversation/_read_json 改用 safe_read_json 转 ConversationError；启动迁移捕获所有单人异常 | c8d39b9 |
| R3 | High | 全局锁残留，URL/文件/现实回答阻塞所有人物 | conversation_service(12) + source_service(4) 全局锁改 _person_lock(person_id) | 006675e |
| R5 | High | request_id 未透传前端 | api() 错误携带「错误编号：request_id」 | 34d3073 |
| R7 | Medium | 模型请求 429/5xx/网络错误无重试 | _json_request 对 429/5xx/网络错误重试 1 次（退避 0.5s） | 31851eb |
| R8 | Medium | list_people 过度宽泛 except Exception | 窄化为 (ProductError, ConversationError, PcfmDataError, CognitiveWorkbenchError) | 34d3073 |
| R9 | Medium | pollJob 无超时；启动不清旧任务 | pollJob 加 10 分钟超时；启动 recover_stale 把 queued/running 标记 failed | 34d3073 |
| R10 | Medium | 状态码中文映射不完整 | 补齐 11 个状态映射；未知值兜底改「未知状态」 | 31851eb |
| R11 | Medium | light=True 仍读全量 JSON | light 模式跳过 versions/optimization_candidates/_source_public 构建 | 006675e |
| R4 | High | SQLite 仅 person/job，核心数据仍 JSON | 落地 SourceRepository + VersionRepository + 写路径（资料/版本元数据镜像 SQLite）+ 迁移脚本补齐 | 8393443 |

## 未修复（低优先级，已记录）

| 编号 | 问题 | 说明 |
|---|---|---|
| R12 | DPAPI 跨平台 / 旧前端残留 / 模型路径兼容 | 低优先级。DPAPI 仅 Windows（当前用户环境为 Windows，可用）；app.js + index.legacy.html 保留作为未构建 dist 时的回退参考；模型路径兼容需按具体端点逐一验证。 |

## 验证

- 后端全量回归：python -m unittest discover -s tests → Ran 227 ... OK (skipped=13)
- 前端：npm run build exit 0；npx vitest run 5/5
- 服务：/api/health 200，app_version=0.10.0-simulation-v5，首页返回 Vue3 dist
