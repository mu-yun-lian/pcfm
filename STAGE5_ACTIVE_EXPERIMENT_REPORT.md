# 第五阶段开发报告：主动实验闭环 v2

## 阶段定位

第五阶段解决的是“在已声明的人物参数模型内，下一道结构化题目问什么最有助于降低参数不确定性”。它不调用 LLM，不从语言风格猜测思想，也不自动宣称发现了新的信念、价值或心理机制。

本次修复把上一版只有“选题—核验”的半闭环改为：

```text
已验证模型
  → 无答案候选池
  → 签名选题计划
  → 签名真实结果
  → 顺序更新人物模型
  → 模型绑定计划与结果血缘
  → 使用晚于实验结果的封存集重新验证
  → 从剩余候选重新规划
```

## 后验预测互信息

当前人物参数使用高斯近似：

\[
\theta\sim\mathcal N(\mu,\Sigma)
\]

对于候选二元选择场景 \(x\)，令：

\[
z=x^\top\theta,\qquad
z\sim\mathcal N(m,v),\quad
m=x^\top\mu,\quad v=x^\top\Sigma x
\]

结果满足 \(y\mid z\sim\operatorname{Bernoulli}(\sigma(z))\)。规划目标为参数与新结果之间的后验预测互信息：

\[
I(y;\theta\mid x)
=H_b(q)-\mathbb E_z[H_b(\sigma(z))],
\qquad q=\mathbb E_z[\sigma(z)]
\]

系统用固定 48 点 Gauss–Hermite 积分计算 \(q\)、条件熵和 \(\operatorname{Cov}(z,y)\)。这修复了局部 Fisher 代理在后验很宽、均值远离决策边界时可能严重低估信息并反转题目排序的问题。

## 批量近似与真正自适应

选中一题后，批量规划器用联合矩的高斯投影估计平均后验协方差。令：

\[
c=\operatorname{Cov}(\theta,y)
=\frac{\Sigma x}{v}\operatorname{Cov}(z,y)
\]

则：

\[
\Sigma'
=\Sigma-\frac{cc^\top}{q(1-q)}
\]

这一步是矩匹配的期望协方差近似，不是已经观察到某个具体答案后的精确后验。

- `selection_count = 1`：计划标记为 `adaptive_single_step`。吸收真实结果后重新规划，构成逐题自适应闭环。
- `selection_count > 1`：计划标记为 `outcome_blind_batch_approximation`。它适用于必须一次下发整批题目的场景，但不能被解释成真正的结果条件自适应。

## 内容血缘与重放防护

bundle v7 除事件 ID 和人物—场景 ID 谱系外，还保存每个建模场景的内容设计指纹。指纹绑定人物、数值特征、特征名、选项、领域和设计上下文，并排除 `prediction_at` 这类非设计执行元数据。

因此以下重放都会被拒绝：

- 旧场景只更换事件 ID；
- 旧场景同时更换场景 ID；
- 两个候选只有 `prediction_at` 不同；
- 同一候选设计在候选池中重复出现。

`Scenario` 在构造时会把选项、领域、上下文键和值规范化为字符串，使程序内构造与 JSON 往返产生相同签名内容。

## 签名计划

阶段 6.5 已将该工件升级为 `pcfm-active-experiment-v3`，在下列字段之外额外绑定实际预测模型 ID、版本和参数维度。v2 不再由现行加载器接受。

- 基础模型和人物 ID；
- 完整候选池摘要与候选数；
- 创建时间、配置、积分点数和选择模式；
- 每道入选题的完整结构、预测选择概率和互信息；
- 累计互信息；
- 矩匹配后的预期协方差；
- 独立的协方差熵下降指标；
- 验证者、计划 ID 和 HMAC-SHA256 签名。

计划验证必须再次提供原候选池。系统从基础模型重新执行完整规划并逐字段比较，因此只重算自哈希，或持有验证密钥后对篡改计划重新签名，都不能绕过候选池推导。

## 结果吸收与重新验证

结果账本必须满足：

- 签名有效；
- 人物、题目集合和结果数量与计划一致；
- 每道题的特征、选项、领域和上下文未修改；
- 所有采集时间晚于计划创建时间。

`apply_active_experiment_results` 还会：

1. 重新推导并核验计划；
2. 按时间顺序逐条吸收真实结果；
3. 把 `plan_id` 累积写入新模型的 `experiment_plan_ids`；
4. 记录结果事件 ID、结果账本摘要和实际协方差熵下降；
5. 要求封存验证账本的所有时间均晚于最后一条实验结果；
6. 每次更新后重新运行既有的独立验证门禁。

CLI 对应命令为：

- `plan-experiment`：生成签名计划；
- `verify-experiment`：重算计划并核验结果；
- `apply-experiment`：吸收结果，保存新模型与训练账本，并完成未来封存验证。

## 验证

当前全项目回归：111/111；本报告对应的第五阶段测试仍为 18/18。

第五阶段：18/18，覆盖：

- 宽后验下的互信息数值与排序回归；
- 规划确定性和候选输入顺序不变性；
- 内部声明目标相对随机批次的优化；
- 独立留出 NLL 效用测试；
- 单步自适应与无结果批量近似的显式区分；
- 计划存储往返、签名、自哈希伪造和重签篡改；
- 场景改 ID 重放、内容重放、元数据去重和规范化序列化；
- OOD、跨域、未验证基础模型和带答案输入拒绝；
- 结果少题、换题、内容修改和倒签拒绝；
- 未来封存验证时间门禁；
- 结果写回、观察数增长、计划血缘和协方差收缩；
- CLI 规划、核验、更新与保存的端到端闭环；
- 模块注册版本。

固定合成外部效用基准中，主动选择 10 题并吸收真实答案后的 200 条留出记录 NLL 为 `0.513229`；100 个等量随机批次的平均 NLL 为 `0.526107`，主动批次胜过其中 94 个。该指标不参与选题目标，因此不是规划器用自身分数证明自身，但它仍只是单个固定合成人物与生成器上的回归基准。

## 尚未解决

- 参数后验仍是 Laplace/高斯近似；Gauss–Hermite 积分准确计算的是该近似下的互信息，不是任意真实后验下的精确互信息。
- 多题计划的矩匹配协方差忽略具体答案分支；只有单题吸收后重规划才是严格的结果条件自适应。
- 当前参数模型只支持固定数值特征下的线性二元选择，不能表示竞争的信念、价值、目标、概念、记忆或因果程序。
- 候选池由外部设计者提供；若题池不包含能区分关键机制的干预，互信息排序无法创造缺失的可识别性。
- 未来验证账本必须由外部真实采集并预先封存；系统能验证时间、签名、独立性和结果，却不能自动生成尚未发生的真实结果。
- HMAC 时间不是外部可信时间戳。掌握共享密钥且能篡改全部原始账本的机构仍超出本原型威胁模型。
- 内容指纹能防止等价设计的简单重标记；若上游改变设计内容并合法签名，仍需要外部实验注册表或透明日志判断其事实身份。
- 当前外部效用证据只来自合成基准；在现实人物研究中仍需多人物、跨时间、跨任务、预注册的外部比较。
