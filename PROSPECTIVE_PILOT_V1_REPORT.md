# Prospective Single-Person Pilot v1 开发报告

## 当前结论

工程状态：`implemented_exploratory`。

现实实验状态：`not_started`。

本阶段没有产生“模型像真人”的新证据。它补齐的是此前缺失的检验工具：先冻结题目、PCFM 概率和三个基线概率，再登记计划，最后只用更晚的真人答案盲评分。

## 已实现

- 单人、单领域、统一特征模式与二选一选项门禁；
- 至少 100 道未来题的不可调弱下限；
- 完整题干 `context.question_text` 绑定；
- PCFM、群体、历史常数和 profile-LLM 四种方法强制齐全；
- 每种预测来源使用 SHA-256 工件摘要；
- 预测概率、题目内容、时间窗和阈值共同进入签名计划；
- 计划签名者、登记签名者和结果签名者三角色分离；
- 结果必须晚于登记、不得晚于预定结束时间；
- 真人 provenance、人物、领域、选项、特征和题干逐项核对；
- 精确重复和特征距离不超过 `1e-6` 的改名近重复题拒绝；
- NLL、Brier、accuracy 和 ECE 由统一评分函数计算；
- 配对 NLL 使用按回答时间排列的 Newey–West/Bartlett 标准误；
- 报告绑定计划、登记回执和完整结果账本，可从原始工件重算；
- `pilot-create`、`pilot-register`、`pilot-score` 三个 CLI 入口。

## 固定成功条件

有效实验只有同时满足以下条件才返回
`passed_prospective_pilot`：

- PCFM NLL 不高于 `0.65`；
- PCFM ECE 不高于 `0.15`；
- 相对三个基线的平均配对 NLL 改善均至少为 `0.01`；
- 三个 Newey–West 95% 区间下界均严格大于零。

合法完成但未满足指标时返回 `completed_no_support`。签名、证据角色、时间、题目或范围不合法时拒绝评分。

## 敌对审计发现并修复

第一次实现后，普通测试全部通过，但敌对检查发现：

1. 可用微小特征扰动和新 ID 把同一道题重复计算；
2. 独立样本标准误忽略同一人物连续回答的时间相关性；
3. 只绑定特征而不绑定完整题干，仍可替换措辞；
4. 任意字符串模型引用不能确定实际模型、提示词和人物资料版本。

对应修复为近重复检测、唯一回答时间、固定 Newey–West 修正、题干必填和 SHA-256 模型工件摘要。

## 尚不能由代码解决

- 本地 HMAC 不能证明三个角色属于独立现实组织；
- 本地时间字符串不能代替可信时间戳；
- 签名不能证明上游登记方没有隐藏其他试验；
- profile-LLM 的模型、提示词和人物资料必须由外部工具生成并冻结；
- 仍需要真实人物实际完成至少 100 道注册题；
- 即使实验通过，也只支持该领域、该题集和该时间窗内的概率预测优势。

因此门禁不得标为 `implemented_confirmatory`。真正确认仍需要外部追加式登记、独立持钥人和真实未来数据。

## 验证结果

- 编译与导入检查：通过；
- 本阶段聚焦测试：`15/15`，耗时 `1.371s`；
- 全项目回归：`198/198`，耗时 `186.841s`；
- 确认门禁：按设计拒绝，等待真人未来留出、基础模型资格核验和局部适用性证据。

这些结果证明试验工具满足当前合同，不证明 PCFM 已经能够预测真人。
