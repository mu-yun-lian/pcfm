# 第四阶段修复报告：动态残差状态 v2

## 修复对象

第四阶段只处理“稳定人物参数之外，是否存在可预测的短期行为残差”。它不从残差生成心理标签，也不把预测提升解释为情绪、信念或人格变化。

审核发现 v1 存在五个实质性漏洞：

1. 报告只有可重新计算的自哈希，篡改者可修改内容后重算哈希。
2. 适用性数据未完整进入模型谱系，旧试验可换事件 ID 后重放。
3. 配置与监测窗口没有独立预注册，存在看完数据后调参或截窗的空间。
4. HAC 区间被当成可反复查看数据的判定门槛，没有序贯误报控制。
5. 动态状态可直接带入未验证领域，第一事件先验也忽略与基础证据的真实时间间隔。

v0.6.1 已逐项修复。

## 数学模型

固定人物参数 \(\theta\)：

\[
y_t \sim \operatorname{Bernoulli}
\left(\sigma(x_t^\top\theta+z_t)\right)
\]

\[
z_t=\rho(\Delta t)z_{t-1}+\epsilon_t,\qquad
\rho(\Delta t)=\exp\left(-\log(2)\frac{\Delta t}{h}\right)
\]

Bernoulli 观测使用一维拉普拉斯近似更新。人物参数协方差进入 logistic-normal 概率近似，避免把已知参数不确定性全部归入动态状态。

第一条监测事件的先验从基础模型最后参考时间传播：

\[
V_1^-=\rho^2 V_0 + V_\infty(1-\rho^2)
\]

长时间没有观察时，状态不确定性因此向稳态方差回归。

## 逐事件证据

当前事件结果只用于更新下一时点状态。每个事件先产生冻结人物模型的 `static_probability` 和只使用过去结果的 `prequential_probability`。

报告从概率与真实选择重新计算所有 NLL、HAC 区间、e-value 和连续检出长度。任何派生字段与原始点不一致都会加载失败。

`prequential_residual_signal` 需要：

- 样本数不少于 `minimum_samples`；
- 平均 NLL 提升不少于 `minimum_nll_uplift`；
- 历史最大 log e-value 不小于 `-log(sequential_alpha)`；
- 状态效应和连续检出长度达到预注册阈值。

默认 `sequential_alpha=0.05`。HAC 区间仅作描述，不再作为序贯门禁。

## 预注册、谱系和签名

`pcfm-dynamic-state-plan-v1` 在监测前固定模型、人物、窗口、事件数和全部配置，并由验证机构签名。

推断必须满足：

- 注册时间早于窗口开始；
- 事件数和首尾时间与计划一致；
- 模型、人物和配置与计划一致；
- 所有事件签名有效且时间严格递增；
- 事件晚于基础模型全部参考证据；
- 事件及人物—场景试验不属于模型已有谱系；
- 所有监测点位于已验证适用域。

当前 bundle v7 把训练、适用性与验证三类事件、试验内容指纹及主动实验计划血缘纳入模型身份。

`pcfm-dynamic-state-v2` 报告也由验证机构签名。正式预测必须同时提交原始监测账本和计划；系统重新运行推断并逐字段比较报告。因此，修改报告后重算哈希或重新签名都不能脱离原始证据生效。

## 迁移限制

动态状态只能用于与监测证据相同且已验证的领域元数据。未知领域、选项或上下文默认拒绝 `dynamic_state_transfer_unvalidated`。

显式 `state_override` 只用于诊断，并记录覆盖原因、撤销概率区间，将模型形式不确定性标为 `unquantified_override`。

## 验证结果

当前全项目回归：111/111。阶段四自身：20/20。

攻击性回归覆盖：

- 自哈希重算不能伪造签名报告；
- 重签篡改报告仍被原始账本重算拒绝；
- 已用于适用性建模的试验换事件 ID 后不能重放；
- 预注册完整窗口不能事后截取；
- 动态状态不能默认跨域迁移；
- 第一先验随基础参考时间到监测首事件的间隔变化；
- 计划内容被修改后，即使重算 `plan_id` 也不能通过原签名。

20 个合成随机种子压力测试：

- 静态误报 0/20；
- logit `+2.2` 暂时偏移检出 20/20；
- logit `+2.5` 暂时偏移检出 20/20；
- `+2.2` 偏移在恢复期末回落 20/20。

这些结果只验证实现和当前合成条件，不是现实人物研究结论。

## 尚未解决

- 动态状态仍是一维无标签残差，不是人物认知状态。
- 观测残差不能区分人物变化、遗漏变量、测量误差和采集偏差。
- HMAC 不能防止掌握共享密钥且能篡改原始账本的验证机构。
- 若上游同时伪造事件身份、场景身份和合法签名，系统没有外部事实来源判断试验是否重放。
- 默认参数尚未用现实人物纵向数据外部校准。
- 当前系统已有受限的结构化主动实验模块，但仍没有多维记忆、信念、价值、目标、概念表示或可识别的因果认知程序。
