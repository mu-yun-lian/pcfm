# PCFM 对话式人物系统 —— 架构与改造基准（定稿）

> 本文档是后续所有改造的唯一对照基准。结论来自与用户的讨论收敛，不包含未经确认的新增内容。

---

## 0. 项目定位

**从真实人物的公开言行中，重建他的「价值判断网络」，让系统用这个网络（而非大模型印象）像他本人一样思考，并用第一人称、他的语言风格说出判断，且每句判断可回溯到证据。**

三个「不是」：不是预测器、不是检索器、不是角色扮演器。

---

## 1. 核心机制链（已定稿，不再动）

```text
① 原始材料分割 → ② 事件原子 → ③ 公开反应倾向 → 价值网络
                                          ↓ 新问题
④ 问题拆特征 → 特征匹配倾向 → 代码推导评价（永远有输出）
                                          ↓
⑤ 外部知识补深度 → ⑥ 渲染层第一人称风格 → 界面分栏
```

---

## 2. 三条铁律（边界，决定所有设计）

1. **原子是计算核心，不是检索索引。** 事件/倾向/知识原子是第一公民，预测从它们推导，不是检索后拼接。
2. **LLM 只出候选，代码做验证 / 聚合 / 门禁 / 最终选择。** LLM 可「理解语言、提候选、组织自然语言」，但不能决定人物立场，不能把候选直接变成事实。
3. **内容与风格分离。** 「把问题和证据缝起来」属于内容组合层，不属于风格层；风格只改表达形式，永远不改观点。

---

## 3. 数据模型

### 3.1 事件原子（最小决策/表态单元）

```text
DecisionEpisodeAtom
- event_id / parent_event_id（复杂事件的父子层级）
- source_id / source_lineage / evidence_spans（逐字跨度）/ content_hash
- time + time_precision（时间及精度）
- domain（领域标签）· event_type（事件类型标签）· role_scope（角色）
- trigger（触发）· available_options（可选方案）
- observed_choice_or_answer（实际选择/回答，逐字）
- protected_interest（保护的利益）· accepted_cost（接受的代价）
- conditions（条件）· exceptions（例外）· stated_reasons（理由）
- review_status（审核状态）· origin（confirmed_direct_evidence_only | reviewed_semantic_event）
```

**缺字段降级规则（不伪造）**：缺时间→可用于历史回答/风格/知识候选，不可用于时间趋势与未来预测；缺理由→保留为「未知理由」，不让 LLM 补编；缺取舍→只存「公开表态」，不强行造取舍。

### 3.2 公开反应倾向（伴随原子，从事件派生）

正式形态是一条可证伪条件句：

> 在事件 E、条件 C、公开角色 R 下，人物公开选择/表达了「A 优先于 B」，证据 = 原文片段 S。

```text
TendencyHypothesis
- tendency_id / source_event_ids（派生自哪些事件原子）
- tendency_type（8 类，见 3.5）
- preferred_side（优先项，封闭 ID）· sacrificed_side（被牺牲项，封闭 ID）
- direction（support | oppose | conditional_support | mixed）
- triggering_conditions / excluded_conditions
- domain_scope / event_type_scope / role_scope / time_scope
- supporting_event_ids / contradicting_event_ids
- alternative_explanations（替代解释）
- extraction_confidence（提取置信度：单事件=低，跨事件=高）
- validation_status（candidate | experimental | validated | active | rejected | expired）
```

**命名纪律**：内部统一叫「公开反应倾向」，不叫「价值观」，防止把行为推断冒充真实心理。

### 3.3 知识原子（人物知识储备）

```text
KnowledgeAtom
- knowledge_id / proposition
- knowledge_type（事实/程序/专业/自传/社会/因果/概念框架）
- person_epistemic_status（explicitly_stated | demonstrably_used | likely_accessible
  | reported_by_other | uncertain | explicitly_unknown）
- external_truth_status / acquired_before / valid_until
- evidence_event_ids / source_spans / contradiction_ids
```

**三个知识空间强制隔离**：外部世界知识 ≠ 人物当时知识 ≠ 本轮对话刚告诉他的知识。

### 3.4 价值网络（修正版）

**节点只有三类**：事件原子、公开反应倾向原子、知识原子。领域 / 事件类型 / 角色是属性标签，不是节点。

**推理边只有 4 类**（真正参与聚合）：

```text
supports_tendency / opposes_tendency / qualifies / contradicts
```

其余不存为边：
- 来源去重（same_underlying_event / near_duplicate）→ 入库时去重；
- 时间顺序（precedes / follows）→ 用于稳定性判断；
- 同领域/同结构（same_domain_as / same_decision_structure_as）→ 查询时按属性 GROUP BY，不存。

**聚合 = 二维网格（事件类型 × 领域），不是线性四层**：

```text
底层：事件级倾向（只解释这一个事件）
中层：网格 = 事件类型 × 领域
      每个格子 = 一个「条件化倾向」，如「政治·风险决策」这一格
顶层：跨格子的稳定倾向（同一规律在多个格子反复出现 + 反例检查）
```

**「隐形倾向」不是独立机制**，就是二维网格的自然输出：同一决策结构，在「公共·风险」格子方向是保守、在「个人职业·风险」格子方向是激进。纪律只有一条——这种「跨格子方向相反」的模式，先当候选、拿留出事件验证优于替代解释，再进预测。

**矛盾保留，不平均**：同一人「公共上重集体安全、个人上重自主权」是条件差异，不是噪声。

**倾向原子可重算**：事件是原料，倾向是产物，产物必须能从（事件原子 + 提取规则）重新算出来。

### 3.5 倾向类型（8 类公开反应倾向）

1. 对对象的支持 / 反对 / 混合评价
2. 行为原则和优先级
3. 条件性政策偏好
4. 手段—目的判断
5. 责任归属
6. 风险容忍度
7. 对规则 / 程序 / 结果 / 公平的权衡
8. 对人物行为方式的评价

例外和反证作为元数据，不单独成类。

### 3.6 封闭价值分类 ID

优先项 / 被牺牲项的取值来自预定义封闭分类（如 safety / speed / fairness / competence / institutional_norms / rule_of_law / national_security …）。LLM 只能建议同义映射（如「安全」和「降低伤害」同类），代码只接受预存在或人工确认的映射。

---

## 4. 四块改造细节

### 4.1 原始材料分割
- 单位是「一个最小回应情境」，不按字数切（一段回答可跨多段、一个短句也是一个情境）；
- 一段回答 = 一个事件候选；多个取舍 = 多个伴随原子，不因取舍复制事件；
- 说话人逐句归属；正文 / 引语 / 他人转述分离，只有本人表达能成标签；
- 分工：LLM 提「边界+说话人+事件类型」候选 → 代码校验逐字片段真实存在 → 歧义人工确认；
- 产出：分割候选 + 逐字 span + 产出率报告。

### 4.2 原子事件提炼
- 补全 3.1 全部字段，缺字段按降级规则处理；
- 正式原子必须「身份确认 + 逐字可定位 + 边界明确 + 谱系明确 + 时间已知或标记缺失 + 审核=确认」才入库；
- 产出：正式事件原子 Schema + 审核门禁。

### 4.3 价值网络倾向
- 倾向原子扩大为 8 类公开反应倾向（不限二元取舍）；
- 单事件即产出低置信度取向原子，跨事件重复再聚合增强（不等两份材料）；
- 按「事件类型 × 领域」二维网格聚合；
- 封闭价值分类 ID + 同义映射门禁；
- 产出：倾向原子 Schema + 聚合门禁 + 分类词表。

### 4.4 渲染层语言风格
- 冻结内容（立场锚点 + 允许事实 + 逐字引用 + 边界）→ 第一人称风格化；
- 风格档案只存表面语言特征（句式/词汇/句长/节奏/修辞/开场收束/场合差异/表达强度），禁止存信念、价值观、目标、决策方式、未知问题观点、事实记忆、未来行为、完整扮演提示词；
- 渲染门禁：改措辞不改观点；渲染后代码检查立场/事实/数字/专有名词/逐字引语语义是否被改，改了回退中性表达；
- 产出：风格档案 + 渲染门禁。

---

## 5. 运行逻辑（用户输入 → 回答）

1. **问题拆特征**（LLM 候选 + 代码校验）：在问谁 / 评价什么对象 / 涉及什么行为·政策·价值冲突 / 时间·场景 / 与前文什么相关 / 问题粒度（宽·窄·复合）；
2. **特征 → 倾向匹配**：目标的可观察特征（政党、政策立场、公开言行、执政履历，非人格推断）→ 语义匹配到倾向网络里被触发的倾向原子（不是字符匹配）；
3. **倾向推导评价**：代码据被触发的倾向原子算方向 + 强度 + 依据原子 ID，永远有输出，每句挂原子；
4. **外部知识补深度**：补论证 / 背景 / 事实，但不替人物定立场；
5. **冻结**：产出结构化回答（stance / claims / evidence_refs / applicability / confidence / bridge 元数据），LLM 不能改 stance/claims/数字/引语；
6. **接洽组合**：代码用 bridge 元数据（实体别名/时间/场合）把问题与证据缝起来，逐字引用原样保留；
7. **渲染层**：第一人称 + 人物语言风格（只改措辞）；
8. **界面分栏**：第一人称正文 / 声明 / 可展开依据，三者分离，声明不混入正文。

**预测四路线**：①现实直接回答 → ②同类事件推断（最有价值）→ ③邻近事件/新领域推断（低置信条件式）→ ④无依据（外部知识补，明标非人物立场）。

**问题粒度必须匹配回答粒度**：宽问题（总括评价）必须多维合成，禁止单条窄引用冒充完整看法；窄问题才允许单条证据回答。

---

## 6. 置信度（三层，不是手写单值）

```text
事件有效权重 = 证据合格门禁（硬）
            × 独立性修正（同一现实事件十篇转载只算一个观察）
            × 倾向支持强度（支持 vs 反证）
            × 当前问题适用度（领域/结构/角色/风险/时间相似）
            × 时间有效度（是否过期）
```

三层：事件可信度（来源真实性/说话人/逐字/上下文完整/时间明确/转述/去重）× 倾向推断可信度（自主决定/制度强制/理由本人表达/替代解释/支持强度）× 当前问题适用度。来源/身份/重复先硬门禁，合格事件进概率模型输出区间，不是伪精确「0.4」。

---

## 7. 外部知识库

- 三个来源：大模型知识 / 网络查询 / 人物原始材料；
- 三个空间隔离：外部世界知识 / 人物当时知识 / 本轮对话知识；
- 职责：补论证、补背景、补事实；不替人物定立场。

---

## 8. 路线（实施顺序）

1. 先完成整理改造（四块全部改到位，不是先做切片）；
2. 再建立材料丰富、可核验的人物（含独立留出材料）；
3. 测试整条链：真实留出评估 + 与「通用模型 / 纯检索 / 频率 / 错误人物」基线比较；
4. 诚实标注 not_assessed，直到有真实封存验证。

---

## 9. 可证伪纪律（贯穿，是手段不是目标）

- 门禁先于估算器；失败测试先于实现；
- 证据角色隔离（拟合/校准/发现/选择/封存/监测）；
- 操作化命名（倾向 ≠ 价值观，行为只识别条件偏好）；
- 诚实标注「预测 vs 事实」，测试通过 ≠ 人物预测准确。

---

## 10. 与当前代码的差距（速查）

| 环节 | 当前 | 目标 |
|---|---|---|
| 问题理解 | 正则/关键词（_query） | 拆特征（LLM 候选 + 代码校验） |
| 搜索 | 字符匹配（_similarity/_entity_aliases） | 伴随原子语义匹配 |
| 事件类型轴 | 无（只有 domain_tags） | 冲突/资源分配/风险决策/人员选择/道德评价 |
| 倾向类型 | 无（只有 interest 显著性，craft→quality 误判） | 8 类公开反应倾向 |
| 聚合 | 扁平 single_source_public_salience | 二维网格（事件类型×领域） |
| 网络边 | 零边 | 4 类推理边（supports/opposes/qualifies/contradicts） |
| 推导 | 无（检索到就拼） | 代码据倾向推导，永远有输出 |
| 深度 | 无 | 外部知识库补论证 |
| 风格 | 逐字原样，无风格化 | 渲染层第一人称（不改观点） |
| 输出 | 第三方引用 + 声明拼进正文（重复显示） | 纯第一人称正文 + 声明/依据分栏 |
| 置信度 | 手写 support=0.35+0.08×… | 三层置信度 + 区间 |

---

## 附：本轮定稿的关键修正（相对早期讨论）

1. 分类轴（领域/事件类型/角色）是属性不是节点；
2. 四层线性聚合 → 二维网格（事件类型 × 领域）；
3. 隐形倾向并入网格，不单设机制；
4. 边拆成「4 推理边 + 来源去重 + 时间 + 分组」。