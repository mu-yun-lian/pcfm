# 第六阶段开发报告：竞争性预测机制比较 v2

## 阶段定位

第六阶段开始处理单一线性人物参数无法表达的结构差异，但没有直接进入“信念模型”“价值模型”或“思想模型”。原因是仅凭行为相关性，不能把一个拟合较好的交互项自动命名为真实信念、价值或因果机制。

本阶段实现的是更窄、可检验的命题：

> 在预先声明的有限数学结构集合中，是否有一个候选能在完全独立的最终数据上稳定改善人物选择预测？

通过只能称为 `supported_candidate`，解释范围固定为 `predictive_structure_only`；所有报告都包含 `causal_interpretation_not_identified`。

## 显式候选语言

`MechanismHypothesis` 是一组有限的 `MechanismTerm`。当前只允许：

- `intercept`：常数修正；
- `linear`：单特征线性修正；
- `interaction`：两个不同特征的乘积；
- `absolute`：单特征绝对值；
- `quadratic`：单特征平方。

每个项必须引用稳定模型已有的命名特征。候选不得包含重复项；两个仅改假设 ID、结构完全相同的候选也会被拒绝。

这套语言是结构化数值程序，不调用 LLM，也没有自由文本推断路径。

## 三段证据隔离

`pcfm-mechanism-plan-v2` 必须在证据窗口开始前签名，并固定：

- 基础模型及人物；
- 完整候选集合；
- 拟合正则化、确认阈值和区间配置；
- 发现、选择、确认三个严格有序的时间窗口；
- 每个窗口的精确事件数；
- 验证者与签名。

三份账本承担不同且不可交换的职责：

```text
发现账本
  └─ 拟合所有候选参数与协方差

选择账本
  └─ 比较独立 NLL，只确定一个候选

确认账本
  └─ 不再改变候选，只确认该候选相对基础模型的增益
```

确认集答案完全不进入参数拟合和候选选择。测试中即使把确认答案全部翻转，`selected_hypothesis_id` 仍不变化，只会改变确认指标和最终状态。

## 拟合与选择

稳定人物模型的 logit 均值作为固定 offset：

\[
\eta_i^{(0)}=x_i^\top\hat\theta
\]

候选结构产生项向量 \(\phi_h(x_i)\)。除显式截距外，所有项只用发现集均值和标准差规范化。候选修正为：

\[
\eta_i^{(h)}
=\eta_i^{(0)}
+\tilde\phi_h(x_i)^\top\beta_h
\]

\(\beta_h\) 在发现集上用带 \(L_2\) 正则的 Bernoulli MAP 拟合。所有候选随后在选择集上计算 NLL，并以 `selection_nll`、`hypothesis_id` 的确定性顺序选择一个候选。

确认集只计算已选候选与基础模型之间逐事件 NLL 差：

\[
d_i=\ell_i^{(0)}-\ell_i^{(h^\*)}
\]

选择、确认和正式部署均使用同一个 Logistic-normal 后验预测概率核；不再用 plug-in sigmoid 做确认、再用方差收缩公式部署。报告给出平均改善、预注册区间和确认集校准误差。确认事件时间唯一时使用时间有序 HAC 标准误；时间并列时使用不依赖事件 ID 排序的 IID 标准误。只有同时满足：

- 平均 NLL 改善至少为 `0.01`；
- 95% 区间下界大于零；
- 确认集 ECE 不高于 `0.15`；

才返回 `supported_candidate`，否则返回 `no_supported_candidate`。

上述样本数、效应、置信水平、校准和 180 天报告寿命均有不可向下放宽的硬边界，配置只能更严格。

## 证据与重放门禁

三份账本必须：

- 签名有效；
- 只包含计划人物；
- 精确匹配预注册事件数；
- 全部位于各自窗口；
- 发现、选择、确认按时间严格有序；
- 事件 ID、人物—场景 ID 和内容设计指纹三重不重叠；
- 不复用基础模型训练、适用性或验证谱系；
- 特征结构和适用域已验证；
- 不包含未验证的领域、选项或上下文迁移。

内部计算会按时间和事件 ID 形成确定性数据顺序，因此相同证据的输入顺序不改变报告。时间并列的确认批次不把事件 ID 当作真实时间顺序，标准误对事件 ID 重命名保持不变。

## 可复核报告与使用门禁

`pcfm-mechanism-report-v2` 绑定：

- 计划与基础模型；
- 三份账本的事件 ID 和数据摘要；
- 所有候选的发现集拟合参数、协方差和选择集指标；
- 被选择的唯一候选；
- 确认集基础 NLL、候选 NLL、改善、标准误方法、区间和 ECE；
- 三段证据共同覆盖的领域、选项、上下文以及所选机制项的数值范围；
- 状态、非因果解释范围、报告 ID 和签名。

`predict_with_mechanism` 不信任单独报告。每次正式预测都必须再次提交计划、报告和三份原始签名账本，系统重新执行完整拟合、选择和确认并逐字段比较。报告即使被持密钥者修改、重算哈希并重新签名，只要不符合原始证据推导，仍会被 `mechanism_report_derivation_mismatch` 拒绝。

只有 `supported_candidate` 可进入预测。仅因机制错设失败、但个体化和时间稳定性均通过的基础模型允许由该复合证据修复；个体化失败、未验证或时间不稳定的基础模型仍拒绝。预测还必须位于机制自己的元数据和局部项支持域内，且报告不能超过 180 天。输出同时报告基础概率、修正概率、条件参数区间、候选 ID、计划 ID 和报告 ID。

当前区间把稳定参数协方差和选中修正项的 Laplace 协方差相加，但没有量化：

- 候选集合遗漏；
- 候选选择不确定性；
- 基础参数与修正参数之间的联合相关；
- 模型形式错误。

因此输出强制标记：

- `model_form_uncertainty_status = candidate_selection_not_quantified`
- `uncertainty_scope = conditional_gaussian_parameter_approximation`

## CLI

- `plan-mechanisms`：签名预注册候选和三段窗口；
- `compare-mechanisms`：发现拟合、独立选模、最终确认并保存签名报告；
- `predict-mechanism`：从原始证据重算报告后进行受门禁预测。

## 验证结果

全项目回归：118/118。

第六阶段：17/17，新增覆盖：

- 预设非线性交互—绝对值结构被选择并确认；
- 线性空数据不误报候选支持；
- 确认答案不能改变选择结果；
- 证据输入顺序不影响报告；
- 改 ID 的基础设计重放被内容指纹拒绝；
- 三账本重叠、计数和时间错误被拒绝；
- 未知特征项和 OOD 机制证据被拒绝；
- 计划与报告签名往返；
- 修改并重签报告仍被原始证据重算拒绝；
- 未获支持的候选不能进入预测；
- 正式预测重新计算报告并改善固定留出场景的真实概率误差；
- 模块注册；
- CLI 计划—比较—预测闭环。
- 纯机制错设失败可修复，个体化失败仍拒绝；
- 从训练到确认始终稳定存在的错设可端到端恢复；
- 确认 NLL 与部署概率核逐值一致；
- 置信水平、最小效应、校准和样本数门槛不能调空；
- 机制跨领域、局部项超界和过期报告被拒绝；
- 同时刻批次的标准误不受事件 ID 重命名影响。

固定非线性合成基准：

- 选择：`nonlinear-interactions`；
- 基础确认 NLL：`0.597100`；
- 候选确认 NLL：`0.329305`；
- NLL 改善：`0.267796`；
- IID 区间：`[0.189821, 0.345770]`；
- 确认 ECE：`0.074140`；
- 状态：`supported_candidate`。

固定线性空数据对照：

- 被选候选：`linear-residual`；
- NLL 改善：`0.009278`；
- IID 下界：`-0.022255`；
- 状态：`no_supported_candidate`。

这些结果只证明当前实现能在一个已知合成生成器中恢复预先列出的结构，并在空对照中执行拒绝门禁；它们不是现实人物机制恢复的灵敏度或特异度。

## 仍然存在的功能问题

- 候选语言仍非常有限，不表示多步规划、记忆检索、信念更新、反事实推理或社会递归。
- 系统只能在给定候选中选择，不能证明未列出的结构不存在，也不能证明候选集覆盖真实机制。
- 预测结构相符不等于因果机制相同；没有随机干预或可识别因果图时，多个内部过程可产生相同选择分布。
- 三段窗口和事件数能抑制事后选模，但若上游隐瞒额外采集记录，仍需要外部试验注册表或透明日志证明账本完整。
- HAC 区间依赖弱平稳等近似；现实纵向人物数据可能需要分层、状态空间或稳健块自助法。
- 当前候选只作为外接报告使用，尚未合并进基础 bundle 的长期模型身份，也尚未直接驱动主动实验规划器区分竞争结构。
- 条件参数区间不包含模型选择不确定性，不能当作完整后验可信区间。
- 真实信念、价值和目标模块仍保持预留状态；在出现可操作变量、干预设计和跨任务确认之前不应填充。
