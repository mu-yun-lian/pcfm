# Tyler Cowen Historical Corpus v1 开发报告

## 当前结论

工程状态：`implemented_unintegrated`。

现实数据状态：仅汇编现有的 2026-07-31 官方 RSS 快照，尚未开始历史档案批量采集。

本模块没有生成观点标签，也没有产生“模型像真人”的新证据。它只补齐本地官方快照的重放验证、跨页去重、时间角色分离和语料身份绑定。

## 已实现

- 精确声明全部预期来源 URL，缺少或多出输入均拒绝；
- 每个来源必须携带 `tyler-source-v1` 工件和对应原始 HTML/RSS；
- 从原始快照重新提取并逐字段核对来源工件；
- 来源顺序、计划 URL 顺序和等价时区写法不改变语料身份；
- 跨来源重复 URL、重复 post ID 和相同规范化正文拒绝；
- 每条记录保留来源工件摘要和完整原始结构化帖子；
- 固定分配 `training_discovery`、`protocol_validation` 或 `retrospective_diagnostic`；
- 2026-08-01 起的未登记未来资料硬拒绝；
- 时间边界按半开区间处理，不存在 23:59:59 后的微秒漏洞；
- JSON 往返、内容摘要、CLI 和原始证据重算路径。

## 当前真实工件

- 来源数：1；
- 帖子数：13；
- `needs_human_annotation`：9；
- `ambiguous_not_trainable`：1；
- `no_stance_candidate`：3；
- `retrospective_diagnostic`：13；
- 标注前训练资格：0。

这 13 条记录全部发表于已观察的回顾期。即使后续取得一致人工标签，也不能转为训练数据。

## 为什么尚未标为完成

当前双人标注包 v1 绑定一个 `tyler-source-v1` 来源工件；历史语料库则可能包含许多页面和来源工件。尚未实现一个同时绑定语料库摘要、逐条来源谱系和跨来源完整候选集的双人盲标包。因此该模块不能直接进入标注或训练主路径，状态必须是 `implemented_unintegrated`。

下一工程步骤应升级为多来源标注包，而不是绕过语料库逐页随意标注。升级后必须证明：每个候选恰好出现一次、A/B 看到相同候选集但不同顺序、每条证据引用仍回到原始来源单元，并且时间角色不能被标注结果改变。

## 仍需外部解决

- 页面清单是否覆盖完整官方档案，需要外部登记和采集记录；
- 页面在批量采集过程中是否移动，需要可信快照或追加式日志；
- 两段不同文本是否语义重复，不能由精确摘要识别；
- 真实双人隔离、标注者身份和专业能力不能由本地程序证明；
- 公开文章是否等于私人决策规则仍不可识别。

## 验证

- 聚焦测试：6/6 通过；
- 编译检查：通过；
- 完整回归：229/229 通过，耗时 196.573 秒；
- 完成门禁：按设计仅因 `implemented_unintegrated` 和缺少统一多来源标注入口而拒绝。
